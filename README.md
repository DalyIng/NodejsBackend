# Nodejs, PassportJS, JWT, BcryptJS, Mongoose, Nodemon, Express

This project is anauthentification, register, crud RESTAPI....

## Development server

Run `npm run dev` for a dev server. Navigate to `http://localhost:3000/`. AND GO TO POSTMAN.
